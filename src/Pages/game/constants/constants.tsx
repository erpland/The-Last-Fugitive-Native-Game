console.log("start width= ", window.outerWidth, "calc = ", window.innerWidth /13)
export const CLIENT_HEIGHT = window.innerHeight;
export const CLIENT_WIDTH = window.innerWidth;
export const TILE_SIZE  = (CLIENT_WIDTH / 13) 